spring:
  application:
    name: jcarsoup
  datasource:
    url: jdbc:mysql://localhost:3306/cargr_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: cargr_user
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8080